class TravelRoute {
    constructor () {
        this.destinations = [];
    }

    addDestination(userId, geolocationResult, room) {
        
    }




}

module.exports = {Destinations};


// User joins a NEW room
// Directs to create location page
// Form takes in address OR zip
// Creates a destination and adds to Destinations array
// Allows user to add more
// When finished, redirects to room
// Displays route separate by dots.
// Show current location highlighted.